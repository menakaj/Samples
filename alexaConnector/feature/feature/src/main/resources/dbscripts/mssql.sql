
-- -----------------------------------------------------
-- Table `alexa_DEVICE`
-- -----------------------------------------------------
CREATE  TABLE IF NOT EXISTS alexa_DEVICE (
  alexa_DEVICE_ID VARCHAR(45) NOT NULL ,
  DEVICE_NAME VARCHAR(100) NULL DEFAULT NULL,
  PRIMARY KEY (alexa_DEVICE_ID) );

